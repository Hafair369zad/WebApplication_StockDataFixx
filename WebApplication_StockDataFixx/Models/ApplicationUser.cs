using Microsoft.AspNetCore.Identity;

namespace WebApplication_StockDataFixx.Models
{
    public class ApplicationUser : IdentityUser
    {
        // Add custom properties and methods for your ApplicationUser if needed
    }
}
